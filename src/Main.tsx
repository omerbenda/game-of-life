import React from 'react';
import ReactDOM from 'react-dom/client';
import AppPage from './Pages/AppPage/App.tsx';
import './Main.css';

ReactDOM.createRoot(document.getElementById('root')!).render(
  <React.StrictMode>
    <AppPage />
  </React.StrictMode>
);
